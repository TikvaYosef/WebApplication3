@*@for (int i = 0; i < @Model.Count; i++)
    {
        <h2>@Model[i].fName</h2>
        <h2>@Model[i].lName</h2>
        <h2>@Model[i].yearOfBirth </h2>
        <h2>@Model[i].class_</h2>
    }*@

@foreach (var item in Model)
{
    <h2>@item.fName</h2>
    <h2>@item.lName</h2>
    <h2>@item.yearOfBirth</h2>
    <h2>@item.class_</h2>

}
